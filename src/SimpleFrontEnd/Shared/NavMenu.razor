<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">SimpleFrontEnd</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="gameservers">
                <span class="oi oi-list-rich" aria-hidden="true"></span> GameServers
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="allocate">
                <span class="oi oi-plus" aria-hidden="true"></span> Allocate
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="connect">
                <span class="oi oi-arrow-right" aria-hidden="true"></span> Connect
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="call-service">
                <span class="oi oi-arrow-circle-right" aria-hidden="true"></span> CallService
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="agones">
                <span class="oi oi-beaker" aria-hidden="true"></span> Agones
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
